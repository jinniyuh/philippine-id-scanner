# Protect includes directory from direct access
<FilesMatch "\.(php|inc)$">
    Order Allow,Deny
    Deny from all
</FilesMatch>

# Allow access only from localhost (if running locally)
# Comment out these lines on production
# Allow from 127.0.0.1
# Allow from ::1

